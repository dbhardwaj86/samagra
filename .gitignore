# --- secrets & local config (PUBLIC REPO: never commit) ---
.env
config.local.py
*.secret
secrets/
*token*.json
credentials*.json
client_secret*.json

# --- generated data (content/state stay local, never in git) ---
samagra.db
*.db
state/
build/
dist/
exports/
lectures_out/
.serve-logs/

# --- claude local settings ---
.claude/settings.local.json

# --- python ---
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.pytmp/
.pytest_tmp/
.mypy_cache/
*.egg-info/
.coverage

# --- codex review run logs (keep reports + runbook, drop raw run logs) ---
docs/codex-reviews/*.log

# --- frontend (node / vite) ---
node_modules/
frontend/dist/
frontend/.vite/
frontend/coverage/
*.tsbuildinfo

# local design reference (extracted handoff zip — read-only, not source)
.design-ref/
Web OS GUI design.zip

# --- cloudflare tunnel (Phase B): commit only the non-secret config.yml ---
*.pem
cloudflared/*.json
mcd-cloud.json
